meta {
  name: RemoveJobSkill
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/employer/jobs/{{job_id}}/skills
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{employer_access_token}}
}

body:json {
  {
    "name": "TypeScript"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
