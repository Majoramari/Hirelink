meta {
  name: UpsertJobLanguage
  type: http
  seq: 7
}

post {
  url: {{base_url}}/employer/jobs/{{job_id}}/languages
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{employer_access_token}}
}

body:json {
  {
    "name": "French",
    "minimumProficiency": "INTERMEDIATE",
    "required": false
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
