meta {
  name: UpdateJob
  type: http
  seq: 4
}

put {
  url: {{base_url}}/employer/jobs/{{job_id}}
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{employer_access_token}}
}

body:json {
  {
    "title": "Backend Engineer (Updated)",
    "description": "Build APIs and maintain them",
    "location": "Remote",
    "jobType": "FULL_TIME",
    "experienceLevel": "JUNIOR",
    "salary": 2500,
    "requiredSkills": [
      {
        "name": "NodeJS",
        "required": false
      }
    ],
    "requiredLanguages": [
      {
        "name": "English",
        "minimumProficiency": "INTERMEDIATE",
        "required": true
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
