meta {
  name: RemoveJobLanguage
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/employer/jobs/{{job_id}}/languages
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{employer_access_token}}
}

body:json {
  {
    "name": "French"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
